bundle:
  name: sales-intelligence-dashboard

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: ${var.DATABRICKS_HOST_DEV}

  prod:
    mode: production
    workspace:
      host: ${var.DATABRICKS_HOST_PROD}
