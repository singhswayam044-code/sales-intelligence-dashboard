resources:
  dashboards:
    sales_performance:
      display_name: "Sales Performance Dashboard"
      file_path: ../src/dashboards/sales_performance.lvdash.json
      warehouse_id: ${var.warehouse_id}
      embed_credentials: false
      catalog: ${var.catalog_name}
      schema: ${var.schema_name}
